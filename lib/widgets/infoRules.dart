import 'package:flutter/material.dart';

class InfoRules extends StatelessWidget {
  const InfoRules({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Race Rules will be displayed here'));
  }
}
